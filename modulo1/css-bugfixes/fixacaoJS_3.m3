```
Exercícios de Fixação de Javascript - Treino 3
    let mp = Number(ex *1 + p1 *2 + p2 *3) / 6
    console.log(mp)
    if( mp >= 9 ){
      return `A`
    }else if((mp < 9.0) && (mp>= 7.5)){
      return `B`
    }else if((mp < 7.5) && (mp>= 6)){
      return `C`
    }else if(mp < 6){
      return `D`
    }
    
  }
```